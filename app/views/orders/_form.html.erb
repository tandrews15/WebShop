<%= form_with(model: order, local: true) do |form| %>
<% if order.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

  <ul>
    <% order.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>


<div class="form-group">
  <%= form.label :shipping_address %>
  <%= form.text_field :shipping_address, class: "form-control"%>
  <%#= form.text_field :shipping_address, value: current_user.shipping_addres, class: "form-control"%>

</div>

<div class="form-group">
  <%= form.label :user_notes %>
  <%= form.text_area :user_notes, class: "form-control"%>
</div>

<div class="form-group">
  <%= form.label :shipping_notes %>
  <%= form.text_area :shipping_notes, class: "form-control" %>
</div>

<div class="form-group">
  <%= form.label :confirmed %>
  <%= form.check_box :confirmed, class: "form-control"%>
</div>

<div class="form-group">

  <%= form.label :payment_id %>
  <br>
  <%= form.collection_select(:payment_id, Payment.all, :id, :name, html_options: {class: "form-control", id:"exampleFormControlSelect1"})%>
</div>

<%= form.hidden_field :user_id, value: current_user.id %>

<div class="form-group">
  <%= form.submit nil, class: "btn btn-success"%>
</div>
<% end %>